# Optional full local stack; standalone ML API does not need these values.
# Fill in .env locally. Keep the two JWT secrets different.
POSTGRES_DB=postgres
POSTGRES_USER=postgres
POSTGRES_PASSWORD=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
